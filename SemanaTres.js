/*Ejercicio de numero pares */

let numpar = (index) => {
    for (let index = 0; index < 20; index++) {
        if (index % 2 === 0) {
            console.log(index);
        }
    }
}
numpar();